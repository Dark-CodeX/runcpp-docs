# Version

## `--version`, `-v`

`--version`, `-v` flag can be used as the following:

```bash
$ runcpp --version
```

Which will show the current version of the application:

```bash
runcpp: v1.0.0
```